#ifdef BUILD_RUST_COMPONENTS
#include "strings_rust.h"
#include <iostream>

// Simple shim implementation — developers can adapt these for use inside C++ classes
// For example, to use the Rust interner inside a Binaryen C++ class, call
// BinaryenStringInternerCreateWrapper() and BinaryenStringInternerInternWrapper().

// Nothing else here — it's a template file for teams to switch to Rust.
#endif
